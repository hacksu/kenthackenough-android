<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/dark_grey</item>
        <item name="colorAccent">@color/red</item>
        <item name="android:actionBarStyle">@style/ActionBarTheme</item> -->
    </style>

    <style name="ActionBarTheme" parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:displayOptions">useLogo|showHome</item>
    </style>

    <!-- Styling the action bar -->
    <style name="Widget.KHE.Toolbar.Solid" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/dark_grey</item>
        <item name="titleTextAppearance">@color/white</item>
    </style>

    <style name="ThemeOverlay.KHE.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <style name="TabLayoutTheme" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/TabTheme</item>
        <item name="tabSelectedTextColor">@color/white</item>
    </style>

    <style name="TabTheme" parent="TextAppearance.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
    </style>



</resources>
